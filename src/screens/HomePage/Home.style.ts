import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {overflow: 'hidden', flex: 1},
  spaceXLogo: {height: 60, width: 200},
  launchHeader: {
    fontFamily: 'NunitoSans-Bold',
    fontSize: 18,
    paddingLeft: 24,
    fontWeight: '800',
  },
});
